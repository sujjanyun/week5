// Named Function
function logToConsole(argument) {
    console.log(argument);
}

// Immediately Invoked Function Expression
// IIFE
(function () {
    const foo = "Foo";
    logToConsole(foo);
})();

const myFunction = => {}

console.log(foo);